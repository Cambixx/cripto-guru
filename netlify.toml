[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  # Extend timeout for long-running scans
  timeout = 26

[build.environment]
  NODE_VERSION = "20"
